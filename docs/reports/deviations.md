# AnyDexRT 偏离清单

论文原方口径来源：AnyDexRT 论文的映射目标、锚点损失公式与训练段落（[arXiv:2607.08341](https://arxiv.org/abs/2607.08341)）。本文档只登记现有实现与该口径的差异，不改变训练默认值或既有产物。

| 偏离内容 | 论文原方口径 | 发现日期 | 处置状态 |
| --- | --- | --- | --- |
| `w_chamfer=80.0`（`geort/trainer.py:495,1038`） | 映射目标为 `L_P-Chamfer + L_dist + L_motion + L_anchor`，论文未给出该项 80 倍的单独调权。 | 2026-07-17 | `c2e` 线显式使用 `w_chamfer=1.0`；CLI 默认 `80.0` 保留，未在本案改动。 |
| `L_curv`，`w_curvature=0.1`（`geort/trainer.py:497,933,1042`） | 论文映射目标未列曲率损失项。 | 2026-07-18 | 保留，未处置。 |
| 映射器内 `L_pinch`，`w_pinch=1.0`（`geort/trainer.py:501,937,1047`） | 论文将捏合处理为独立的 BCE 接触分类与推理期模板精修，不是映射器损失项。 | 2026-07-18 | 保留，未处置。 |
| `epoch=200`（`geort/trainer.py:492,1039`） | 论文训练段落记载 mapper 与接触分类器训练 20 epochs。 | 2026-07-18 | 保留，未处置。 |
| 锚点 `L_align` 采用 `F.mse_loss`（`geort/trainer.py:803`） | 论文 Eq. (4) 为锚点 TIP 的逐样本欧氏 L2 范数均值，而非平方 MSE。 | 2026-07-18 | 保留，未处置。 |
